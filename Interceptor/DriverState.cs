namespace Interceptor
{
    /// <summary>
    /// Determines the current state of Interception Driver.
    /// </summary>
    public enum DriverState
    {
        Installed,
        NotInstalled,
        RebootRequired
    }
}
